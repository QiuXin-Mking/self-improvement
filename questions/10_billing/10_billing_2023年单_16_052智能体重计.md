# q
该智能体重计主要由哪些硬件模块组成？
# a
硬件组成包括：
- lcd1602 液晶显示模块
- 51单片机最小系统
- HX711 模数转换模块及对应的桥式传感器
- 蓝牙模块 HC-05

# q
体重计初始化时为什么需要关闭和开启全局中断？
# a
为了确保 IIC 通信的时序不被打断，在读取毛皮重量（去皮）前关闭全局中断，读取完成后再开启全局中断。

# q
该体重计占用了哪些外设资源？
# a
- 8+3 个引脚（可能指 LCD 数据与控制的 11 个引脚）
- IIC 通讯占用 2 个引脚
- 串口通信占用 RX 和 TX 引脚

