# q
如何使用 AWS CLI 查询指定安全组的入站规则？
# a
使用 `aws ec2 describe-security-groups` 命令，通过 `--group-ids` 指定安全组 ID，并用 `--query` 过滤出 `IpPermissions` 字段：
```bash
aws ec2 describe-security-groups --group-ids sg-0783450f5d3042229 --query "SecurityGroups[0].IpPermissions"
```

# q
如何用 AWS CLI 为安全组添加允许 SSH、HTTP、HTTPS 的入站规则？
# a
使用 `aws ec2 authorize-security-group-ingress` 命令分别放通对应端口（22、80、443）：
```bash
# 放通 22 端口（SSH）
aws ec2 authorize-security-group-ingress --group-id sg-0783450f5d3042229 --protocol tcp --port 22 --cidr 0.0.0.0/0

# 放通 80 端口（HTTP）
aws ec2 authorize-security-group-ingress --group-id sg-0783450f5d3042229 --protocol tcp --port 80 --cidr 0.0.0.0/0

# 放通 443 端口（HTTPS）
aws ec2 authorize-security-group-ingress --group-id sg-0783450f5d3042229 --protocol tcp --port 443 --cidr 0.0.0.0/0
```

# q
如何创建 EC2 密钥对并将私钥保存到本地文件？
# a
使用 `aws ec2 create-key-pair` 命令生成密钥对，指定密钥名，查询密钥材料并输出为文本，保存到文件并修改权限：
```bash
aws ec2 create-key-pair --key-name my-key --query 'KeyMaterial' --output text > my-key.pem
chmod 600 my-key.pem
```

# q
如何使用 AWS CLI 启动一个带公网 IP 的 EC2 实例并仅输出实例 ID？
# a
使用 `aws ec2 run-instances` 命令，指定 AMI ID、实例数量、实例类型、密钥对、安全组，并添加 `--associate-public-ip-address` 分配公网 IP，最后用 `--query 'Instances[0].InstanceId'` 只返回实例 ID：
```bash
aws ec2 run-instances \
  --image-id $AMI_ID \
  --count 1 \
  --instance-type t2.micro \
  --key-name my-key \
  --security-group-ids sg-0783450f5d3042229 \
  --associate-public-ip-address \
  --query 'Instances[0].InstanceId'
```

