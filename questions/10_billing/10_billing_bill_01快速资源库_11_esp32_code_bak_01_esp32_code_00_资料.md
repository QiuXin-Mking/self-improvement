# q
在 ESP32 的 Blinker 示例中，如何通过 App 按键控制串口2发送命令字？
# a
在 `13_节水系统_串口2_按键控制.ino` 和 `14_晾衣架系统_串口2_按键控制.ino` 等示例中，利用 Blinker 库，当 App 上对应的按键被按下时，ESP32 会通过串口2发送相应的命令字。

# q
`07_oled_dht11_blinker.ino` 示例实现了哪些功能？
# a
该示例通过 OLED 显示信息，并集成 Blinker 系统，主要功能包括：App 按键控制串口2发送命令字；将温湿度数据通过滑块上报到 App。

