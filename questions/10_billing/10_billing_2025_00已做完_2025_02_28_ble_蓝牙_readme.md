# q
使用 ESP32C3 通过 BLE 实现文件传输到 SD 卡时，为何需要对文件进行封包处理？
# a
因为 BLE 传输有 MTU（最大传输单元）限制，通常单次传输数据量较小，无法直接发送完整文件。文件数据需要分成多个数据包（封包），按照约定的顺序进行发送、接收和重组，以保证数据完整性并支持断点续传等队列管理。

# q
在这种方案中，手机 APP 负责哪些文件管理操作，而播放控制由谁负责？
# a
手机 APP 负责管理 TF 卡上的文件：获取所有歌曲名称列表、下载文件、删除指定歌曲等。播放控制（如播放、暂停、切歌）不是手机 APP 直接控制，而是由设备上的物理按键控制。

