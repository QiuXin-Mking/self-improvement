# q
在 Arduino IDE 中进行 ESP32 全量烧录时，应选择哪个开发板？
# a
选择 `ESP32 Dev Module`（工具 > 开发板 > ESP32 Dev Module）

# q
ESP32 全量烧录前，为确保彻底擦除旧固件，需要在 Arduino IDE 中勾选哪个菜单选项？
# a
打开菜单：工具 > Erase All Flash Before Sketch Upload，勾选该选项。

