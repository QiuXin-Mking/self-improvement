# q
如何通过 pip 安装 Flask 框架？
# a
```bash
pip3 install flask
```

# q
如何将 `Image-caption-app-V1.1.zip` 解压到当前目录的 `images` 文件夹下？
# a
```bash
unzip Image-caption-app-V1.1.zip -d ./images
```

# q
如何进入解压后的 `images` 目录？
# a
```bash
cd ./images
```

