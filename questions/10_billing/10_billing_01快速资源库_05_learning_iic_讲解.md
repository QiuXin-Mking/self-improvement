# q
IIC 和 I2C 是什么关系？
# a
IIC 等同于 I2C（Inter-Integrated Circuit），两者是同一个总线协议的不同缩写。

# q
I2C 总线中的时钟线通常被称作什么？
# a
时钟线通常标为 SCL 或 SCK，全称是 Serial Clock，用于同步数据传输。

# q
I2C 总线中的数据线有哪些常见名称？
# a
数据线常标为 SDA（Serial Data），有时也写作 SDIN，表示 Serial Data In（串行数据输入）。

