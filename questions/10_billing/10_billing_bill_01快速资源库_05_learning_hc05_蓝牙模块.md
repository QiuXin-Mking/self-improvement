# q
如何将HC05蓝牙模块配置为从机模式，使其能被手机识别？
# a
发送AT指令：
```
AT+ROLE=0
```
注意：必须配置为从机（ROLE=0），手机才能搜索到蓝牙模块。

# q
如何将HC05蓝牙模块的串口波特率设置为115200，无校验，无流控？
# a
发送AT指令：
```
AT+UART=115200,0,0
```

# q
如何查询蓝牙模块的当前角色、地址和绑定地址？
# a
分别使用以下查询指令：
- 角色查询：`AT+ROLE?`
- 本机地址查询：`AT+ADDR?`
- 绑定地址查询：`AT+BIND?`

