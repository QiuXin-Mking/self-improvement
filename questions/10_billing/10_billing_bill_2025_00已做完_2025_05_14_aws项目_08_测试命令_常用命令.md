# q
如何查询 Auto Scaling 组 "webapp-asg" 中的实例列表？
# a
```bash
aws autoscaling describe-auto-scaling-groups \
   --auto-scaling-group-names webapp-asg \
   --query 'AutoScalingGroups[0].Instances'
```

# q
如何获取指定 EC2 实例的公网 IP 地址？
# a
```bash
aws ec2 describe-instances \
  --instance-ids i-04f389d687b74f455 \
  --query 'Reservations[0].Instances[0].PublicIpAddress' \
  --output text
```

# q
如何查看所有 ELBv2 负载均衡器的名称、DNS 域名和 ARN？
# a
```bash
aws elbv2 describe-load-balancers \
   --query 'LoadBalancers[*].[LoadBalancerName,DNSName,LoadBalancerArn]' \
   --output table
```

# q
如何为启动模板创建新版本并更新 Auto Scaling 组以使用新版本，然后启动实例刷新？
# a
创建新启动模板版本：
```bash
aws ec2 create-launch-template-version \
  --launch-template-id lt-0cf76dc39448990ab \
  --source-version 5 \
  --launch-template-data "UserData=$(base64 < userdata.sh | tr -d '\n')"
```
更新 Auto Scaling 组使用新版本：
```bash
aws autoscaling update-auto-scaling-group \
--auto-scaling-group-name webapp-asg \
--launch-template "LaunchTemplateId=lt-0cf76dc39448990ab,Version=6"
```
启动实例刷新：
```bash
aws autoscaling start-instance-refresh \
  --auto-scaling-group-name webapp-asg
```

# q
如何从命令行连接到 RDS MySQL 数据库 "image_caption_db"？
# a
```bash
mysql -h imageappdb.czdhlnsaqrsi.us-east-1.rds.amazonaws.com -u admin -p image_caption_db
```

