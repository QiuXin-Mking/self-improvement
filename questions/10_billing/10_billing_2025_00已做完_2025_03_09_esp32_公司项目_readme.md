# q
这个项目的硬件平台是什么？
# a
使用 ESP32-S3-BOX-3B 开发板。

# q
后端服务端的硬件配置是怎样的？
# a
部署在云服务器上，共两台服务器，每台配备一张 NVIDIA RTX 4080 显卡。

# q
系统使用哪些 AI 模型或 token 方式？
# a
使用了 4o token 机制，并部署了一个 32B 参数规模的模型。

# q
硬件端与后端之间的数据交互格式是什么？
# a
硬件端输出 PCM 格式的音频流，并通过 JSON 结构与后端服务通信。

