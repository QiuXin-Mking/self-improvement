# q
模型生成失败并提示使用TTS语音时，应如何解决？
# a
在软件左上角语音设置里勾选“TTS语音”选项。

# q
天问Block软件默认主板是ASRPRO，下载程序到LU-ASR01前需要将主板设置为什么？
# a
需要将主板改为TWEN-ASR，否则可能无法正常下载。

# q
LU-ASR01语音识别模块在AD中的元器件编号是什么？
# a
模块编号为LU-ASR01，配套排针编号为HDR12X1。

