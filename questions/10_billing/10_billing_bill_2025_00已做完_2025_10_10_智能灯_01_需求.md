# q
智能灯系统的主控模块是什么？
# a
STM32 作为主控模块，实现任务控制。

# q
ASR 语音模块需要识别哪些操作词条？
# a
- 打开台灯
- 关闭台灯
- 台灯调到一档
- 台灯调到二档
- 台灯调到三档

# q
按键模块需要实现哪些台灯控制功能？
# a
按键模块支持打开/关闭台灯，以及将台灯调至一档、二档、三档。

# q
人体红外传感器在系统中如何触发台灯？
# a
检测到有人时，台灯自动亮 2 秒。

# q
OLED 模块在系统中的作用是什么？
# a
提供人机交互界面，将系统状态和参数显示在 OLED 上。

