# q
TM4C123GH6PM 微控制器的主频是多少？
# a
主频为 80 MHz。

# q
在 MCP23017 项目中，I2C0 总线的速度应设置为多少？
# a
I2C0 总线速度设置为 100 kHz。

# q
该倒计时定时器需求定义了哪三种工作模式？
# a
三种模式：SET（设置初始值）、COUNT-DOWN（每秒减1倒计时）、STOP（停止计数）。

# q
AHT10 温湿度传感器的数据读取和更新间隔是多长？
# a
每 5 秒读取并更新一次温度和湿度数据。

