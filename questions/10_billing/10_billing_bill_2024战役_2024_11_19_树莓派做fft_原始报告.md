# q
该频谱分析仪项目使用了哪种ADC芯片进行模拟信号采集？
# a
使用 MCP3208 芯片，通过 SPI 接口与树莓派通信。

# q
MCP3208 的时钟引脚 (CLK) 应连接到树莓派的哪个引脚？
# a
连接到 SCLK（串行时钟）引脚，即树莓派 Pin 23（GPIO11）。

# q
系统对采集的数据进行了何种数学处理以获得频谱？
# a
对采样数据执行快速傅里叶变换（FFT）并提取幅频特性，使用数学库中的 FFT 算法。

