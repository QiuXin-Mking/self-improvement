# q
在 CentOS 系统中如何安装 Mosquitto MQTT 服务端并进行基本的订阅测试？
# a
安装命令：
```bash
yum install -y mosquitto mosquitto-clients
```
启动并设置开机自启：
```bash
sudo systemctl start mosquitto
sudo systemctl enable mosquitto
```
检查服务状态：
```bash
systemctl status mosquitto
```
测试订阅主题：
```bash
mosquitto_sub -h localhost -t test/topic
```
配置文件位于 `/etc/mosquitto/mosquitto.conf`。

# q
如何使用 Docker 构建并运行一个 Node.js 应用？请给出 Dockerfile 示例和关键命令。
# a
Dockerfile 示例：
```dockerfile
# 使用官方Node.js镜像作为基础镜像
FROM node:latest

# 创建并设置工作目录
WORKDIR /usr/src/app

# 将package.json和package-lock.json复制到工作目录
COPY package*.json ./

# 安装项目依赖项
RUN npm install

# 将应用程序代码复制到工作目录
COPY . .

# 暴露应用程序运行的端口
EXPOSE 3000

# 定义应用程序启动命令
CMD ["node", "app.js"]
```
构建镜像并运行容器：
```bash
docker build -t my-node-app .
docker run -d -p 80:3000 --name my-node-container my-node-app
```
也可用交互模式挂载当前目录进行调试：
```bash
docker run -it --rm -p 8080:8080 -v "$PWD":/usr/src/app -w /usr/src/app node:latest bash
```

# q
如何初始化一个 Node.js 项目并安装 Express 框架？
# a
初始化项目（生成 package.json）：
```bash
npm init -y
```
安装 Express：
```bash
npm install express
```

# q
拐杖系统的硬件方案包含哪四个功能页面？
# a
- 页面1：按键检测
- 页面2：姿态实时监控
- 页面3：避障模块
- 页面4：测距

