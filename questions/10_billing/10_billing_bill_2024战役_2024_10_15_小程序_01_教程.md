# q
本项目为什么不推荐使用小程序，而建议使用Web端界面？
# a
不建议写小程序，建议用web端界面，通过公网服务器、容器化MySQL、Flask框架和MQTT协议实现功能。

# q
如何使用Docker运行一个限制内存的MySQL容器，并映射3306端口？
# a
```bash
docker run -d \
  --name mysql \
  --memory=512m \
  -e MYSQL_ROOT_PASSWORD=15970759167 \
  -p 3306:3306 \
  -v /var/lib/mysql:/var/lib/mysql \
  -v /var/run/mysql:/var/run/mysqld \
  mysql
```

# q
如何安装Mosquitto MQTT服务器，并测试消息的发布与订阅？
# a
安装：
```bash
yum install -y mosquitto mosquitto-clients
```
启动服务：
```bash
sudo systemctl start mosquitto
sudo systemctl enable mosquitto
```
发布消息：
```bash
mosquitto_pub -h localhost -t test/topic -m "Hello, MQTT"
```
订阅消息：
```bash
mosquitto_sub -h localhost -t test/topic
```

# q
用于存储设备温度数据的 `dev_tem` 表的创建SQL语句是什么？
# a
```sql
CREATE TABLE dev_tem(
    id INT AUTO_INCREMENT PRIMARY KEY,
    dev_name VARCHAR(255) NOT NULL,
    temperature DECIMAL(5,2),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

