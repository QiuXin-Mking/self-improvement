# q
在ESP32-S3 CAM的WS2812示例中，控制LED的数据引脚是哪个？
# a
引脚 48，通过 `#define LEDS_PIN 48` 定义。

# q
该ESP32项目使用的ArduinoJson库版本是多少？
# a
7.2.1。

# q
根据demo迭代记录，case01和case02分别实现了什么功能？
# a
case01：原有的网络单；case02：uart wifi mqtt connect。

