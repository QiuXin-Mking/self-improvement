# q
如何列出 S3 存储桶 buc-250516 中的对象？
# a
```bash
aws s3 ls s3://buc-250516
```

# q
如何查询 Auto Scaling 组 `webapp-asg` 内的所有实例信息？
# a
```bash
aws autoscaling describe-auto-scaling-groups \
   --auto-scaling-group-names webapp-asg \
   --query 'AutoScalingGroups[0].Instances'
```

# q
如何获取指定 EC2 实例（`i-0c764984da40baf4f`）的公网 IP 地址？
# a
```bash
aws ec2 describe-instances \
  --instance-ids i-0c764984da40baf4f \
  --query 'Reservations[0].Instances[0].PublicIpAddress' \
  --output text
```

# q
如何查询负载均衡器对外暴露的域名？
# a
```bash
aws elbv2 describe-load-balancers \
   --query 'LoadBalancers[*].[LoadBalancerName,DNSName,LoadBalancerArn]' \
   --output table
```

# q
如何登录 RDS MySQL 数据库并查看 `captions` 表的前 10 行数据？
# a
登录命令：
```bash
mysql -h imageappdb.czdhlnsaqrsi.us-east-1.rds.amazonaws.com -u admin -p image_caption_db
```
登录密码：`Admin1234!`  
查看表数据：
```bash
SELECT * FROM captions LIMIT 10;
```

