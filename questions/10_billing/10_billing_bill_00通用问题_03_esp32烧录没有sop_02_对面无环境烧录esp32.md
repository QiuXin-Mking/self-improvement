# q
如何从 Arduino IDE 中导出编译好的二进制文件用于无环境烧录 ESP32？
# a
在 Arduino IDE 中，点击菜单 **项目 -> 导出已编译的二进制文件 -> 开始编译**。编译完成后，前往项目目录下的 build 路径，即可找到所有编译生成的文件。

# q
获取编译文件后，如何准备并启动 ESP32 的烧录操作？
# a
进入项目 build 路径，找到所有文件，将文件导入烧录工具并配置对应的烧录地址，然后开始烧录。具体烧录步骤可参照开源烧录方案。

