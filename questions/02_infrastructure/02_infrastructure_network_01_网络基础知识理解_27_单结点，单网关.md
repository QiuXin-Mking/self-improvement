# q
默认网关的作用是什么
# a
默认网关是用于将非本地子网的数据包路由到其他网络的设备。在一台服务器连接到单个网络时，通常只有一个默认网关。

# q
什么是多网关路由
# a
多网关路由是服务器配置多个网关的情况，系统可配置多个路由表，每个路由表对应不同网络接口并可拥有不同的默认网关。

# q
多网关路由通常用于哪些场景
# a
多网关路由通常用于实现负载均衡、故障转移或使特定流量走特定网关。

