# q
如何使用 find 命令在指定目录下递归查找名为 filename.txt 的文件？
# a
```bash
find /path/to/start/directory -name "filename.txt"
```

# q
以下 find 命令的作用是什么？
# a
在 `/path/to/start/directory` 目录及其所有子目录中，查找文件名为 `filename.txt` 的文件。

