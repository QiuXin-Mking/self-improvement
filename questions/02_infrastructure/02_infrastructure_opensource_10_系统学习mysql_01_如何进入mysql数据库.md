# q
如何通过命令行进入MySQL数据库？
# a
执行 `mysql` 命令即可进入交互式命令行环境。

# q
在MySQL中如何查看当前数据库中的所有表？
# a
使用 `show tables;` 命令。

# q
如何查看MySQL表的结构信息，包含哪些字段属性？
# a
使用 `DESCRIBE 表名;` 或 `describe 表名;` 命令显示表结构，返回信息包括 Field、Type、Null、Key、Default、Extra。

