# q
/etc/iproute2/rt_tables 文件的作用是什么？
# a
用于定义用户自定义路由表的标识符与名称的映射，使得后续可以通过标识符或名称引用不同的路由表。

# q
rt_tables 文件中系统保留的路由表标识符及名称有哪些？
# a
255 local（本地路由表）、254 main（默认主路由表）、253 default（默认路由表）、0 unspec（未指定路由表）。

# q
如何在 rt_tables 中添加自定义路由表？
# a
在文件中添加一行，格式为 `标识符  名称`，标识符为整数，通常从 200 开始递增，例如 `200 mytable`。

