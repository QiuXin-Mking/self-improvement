# q
网关地址通常配置在什么设备上？
# a
网关地址通常配置在路由器上。

# q
同一子网内的多台服务器通过交换机连接时，是否需要设置网关？
# a
不需要。只要它们位于同一个子网中，就可以直接通信，无需配置默认网关。

# q
在跨网段通信中，网关的核心作用是什么？
# a
网关是连接两个网络的设备，作用是将数据包从一个网络传递到另一个网络。在静态路由中，网关作为下一跳地址，指定数据包到达目标网络所需经过的设备。

