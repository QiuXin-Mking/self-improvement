# q
如何使用 grep 递归查找包含指定端口号的配置文件？
# a
```bash
grep -rl "8085" *.conf
```
- `grep`: 文本搜索工具
- `-r`: 递归搜索子目录
- `-l`: 只列出包含匹配内容的文件名，不显示匹配行
- `"8085"`: 要搜索的字符串
- `*.conf`: 只搜索扩展名为 `.conf` 的文件

# q
如何查看匹配行在文件中的行号？
# a
```bash
grep -rn "8085" *.conf
```
- `-n`: 显示匹配行在文件中的行号
- 其他参数含义同递归搜索命令

