# q
`lsmod | grep bonding > /dev/null 2>&1` 命令的作用是什么？
# a
检查是否加载了名为 bonding 的内核模块，并丢弃所有输出和错误信息。如果找到了模块则命令执行成功（返回 0），否则失败（返回非 0），可用于脚本条件判断。

# q
命令中 `> /dev/null` 和 `2>&1` 分别起什么作用？
# a
`> /dev/null` 将标准输出重定向到 /dev/null（丢弃正常输出）；`2>&1` 将标准错误重定向到标准输出，从而使错误信息也一并丢弃。

