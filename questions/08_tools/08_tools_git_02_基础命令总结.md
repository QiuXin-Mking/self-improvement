# q
如何将当前暂存区代码提交到分支中？
# a
```bash
git add .
```

# q
如何使用 git 提交代码？
# a
```bash
git commit ""
```

# q
如何执行交互式变基并调整提交次序？
# a
```bash
git rebase -i main
```
该命令启动了交互式（interactive）变基（rebase），使用 `git rebase -i` 可以调整提交次序。

