# q
find 命令的 `-print0` 选项有什么作用？
# a
`-print0` 选项让 `find` 命令以 null 字符（`\0`）分隔输出每个搜索结果，而不是默认的换行符。这样可以在后续处理（如配合 `read -d $'\0'`）时，正确解析包含空格、换行等特殊字符的文件名，避免解析错误。

# q
如何使用 `find` 命令以 null 字符分隔输出搜索结果？
# a
在 `find` 命令末尾添加 `-print0` 选项，例如：
```bash
find ./ -type f -name "*.sh" -print0
```

