# q
正则表达式 `\s\d+\s` 匹配什么样的字符串？
# a
匹配由一个空白字符、一个或多个数字、再一个空白字符组成的子串，例如 " 123 "、" 9 "。

# q
正则表达式 `\s` 在匹配字符串时起什么作用？
# a
匹配任意空白字符，包括空格、制表符、换页符等。

# q
正则表达式 `\d+` 的含义是什么？
# a
匹配一个或多个连续的数字字符，例如 "1"、"1234"、"000"。

