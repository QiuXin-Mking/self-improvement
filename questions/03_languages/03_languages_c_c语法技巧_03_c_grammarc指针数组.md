# q
什么是指针数组？
# a
指针数组是一个数组，其每个元素都是指针变量。例如 `int *ptr[MAX];` 定义了一个包含 MAX 个整型指针的数组。可以通过 `ptr[i] = &var[i];` 将变量的地址赋值给数组元素，然后使用 `*ptr[i]` 访问对应的变量值。

# q
如何理解数组名的指针特性？
# a
数组名（如 `int a[2]` 中的 `a`）是一个指向数组首元素的常量指针，其值为数组在内存中的起始地址，指向的内存区域长度为 `2 * sizeof(int)`。该指针是只读的，不能被赋值或释放（如调用 `free(a)`），否则会导致段错误。

