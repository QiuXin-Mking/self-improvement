# q
C语言中`__FUNCTION__`宏的作用是什么？
# a
`__FUNCTION__` 是一个编译器预定义的宏，用于在函数体内展开为当前函数的名称（不含参数和返回类型）；常用于调试或日志输出，示例：`printf("This is function:%s.\n", __FUNCTION__);`

# q
如何在C程序中获取当前源文件的名称或路径？
# a
使用预定义宏 `__FILE__`，它会在编译时被替换为当前源文件的完整路径（字符串），可用于输出日志或错误定位，示例：`printf("This is file: %s.\n", __FILE__);`

