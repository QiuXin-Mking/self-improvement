# q
ARRAY_SIZE(x) 宏的作用是什么？
# a
计算数组 `x` 的元素个数，公式为 `sizeof(x) / sizeof(x[0])`。

# q
ARRAY_SIZE 宏的实现原理是什么？
# a
利用 `sizeof` 运算符获取整个数组的字节大小，再除以单个元素的字节大小，从而得到数组中元素的个数。

# q
宏定义中为何给参数 `(x)` 和 `(x)[0]` 添加括号？
# a
防止 `x` 是复杂表达式时因运算符优先级导致 `sizeof` 求值错误，确保先对 `x` 整体求值。

