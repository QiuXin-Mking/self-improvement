# q
listen函数的功能是什么？
# a
listen函数将套接字从主动模式转为被动模式，使其能够被动监听客户端的TCP连接请求。调用listen后，服务器套接字进入监听状态，为接受客户端连接做好准备。

# q
listen函数的第二个参数backlog的作用是什么？
# a
backlog参数指定监听队列的最大容量，即内核中等待被accept处理的已完成三次握手的连接队列长度，用于控制服务器能同时接纳的未处理连接数。

# q
listen函数的返回值是什么？
# a
成功时返回0，失败时返回-1并设置errno以指示具体的错误原因。

