# q
文档整理模板的主要整理要求是什么？
# a
整理要求包括：保持原有结构，不删除原有章节和内容；对简略内容进行适当扩展和丰富；使用标准 Markdown 格式，确保标题层级清晰；保留原文档所有要点，不能丢失；必须从第一个标题开始输出完整文档，不能截断。

# q
该模板要求的输出格式有哪些具体规定？
# a
输出完整的 Markdown 文档，使用 `#` 标记一级标题、`##` 标记二级标题、`###` 标记三级标题、`####` 标记四级标题；列表使用 `-` 或 `*`；代码使用反引号包裹；必须包含原文档所有内容，从第一个字符开始完整输出。

