# q
127.0.0.1是什么？
# a
127.0.0.1是回送地址，指本地机，一般用来测试使用。回送地址（127.x.x.x）是本机回送地址（Loopback Address），即主机IP地址，主要用于网络软件测试以及本地机进程间通信，无论什么程序，一旦使用回送地址发送数据，协议软件立即返回，不进行任何网络传输。

# q
回送地址的范围是什么？
# a
回送地址的范围是127.x.x.x，即从127.0.0.0到127.255.255.255。

# q
回送地址的主要用途是什么？
# a
主要用于网络软件测试以及本地机进程间通信。

