# q
AK和SK在云原生环境中分别指什么？
# a
AK 是 Access Key（访问密钥），SK 是 Secret Access Key（秘密访问密钥），用于身份验证和授权，确保对云服务的安全、受控访问。

# q
AK和SK在API请求中起什么作用？
# a
用户通过 API 与云服务交互时，需使用自己的 AK/SK 标识身份并证明权限；云服务根据这些密钥验证请求合法性并决定是否放行。

# q
用户应如何对待自己的AK/SK？
# a
用户必须妥善保管 AK/SK，避免泄露给未经授权的人员，否则可能导致未经授权的访问或数据泄露等安全风险。

