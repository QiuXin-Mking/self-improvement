# q
PXE装机的资料共享路径是什么？
# a
PXE资料位于共享路径 `\\172.17.251.233\exchange\HCI试用版本\PXE_V1.0.5`。

# q
三台超聚变服务器的BMC IP、管理网口IP和后端网口IP分别是什么？
# a
三台服务器的IP配置如下：
- 第一台：BMC IP 172.22.101.60，管理网口IP 172.22.251.60，后端网口IP 192.168.234.60
- 第二台：BMC IP 172.22.101.61，管理网口IP 172.22.251.61，后端网口IP 192.168.234.61
- 第三台：BMC IP 172.22.251.62，管理网口IP 172.22.251.62，后端网口IP 192.168.234.62

# q
服务器的root账号默认密码是什么？
# a
默认root密码为 `nf`，此外还记录有另一个root密码 `wvu7G55U`。

# q
BMC Web管理界面的登录凭据是什么？
# a
BMC Web界面访问地址为 `https://172.22.101.60/#/login`，账号为 `Administrator`，密码为 `Admin@9000`。

