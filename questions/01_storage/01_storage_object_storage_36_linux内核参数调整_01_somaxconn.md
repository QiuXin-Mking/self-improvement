# q
Linux 内核参数 somaxconn 的作用是什么？
# a
somaxconn 用于设定系统允许的最大监听队列长度，即未完成的连接请求（SYN backlog）的最大数量，默认值通常为 128。

# q
在高并发场景下，为什么需要调整 somaxconn？
# a
因为默认值 128 对于高并发应用可能不足，调大该值可以提高服务器的并发处理能力，防止因监听队列满而丢弃连接请求。

