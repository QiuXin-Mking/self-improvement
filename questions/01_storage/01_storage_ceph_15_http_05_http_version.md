# q
HTTP/1.0 存在的主要性能问题是什么？
# a
每个请求和响应都需要建立一个新的 TCP 连接，导致大量网络延迟。

# q
HTTP/1.1 相比 HTTP/1.0 引入了哪些关键优化特性？
# a
连接复用（保持连接打开，在同一连接上处理多个请求和响应）、分块传输编码（允许响应体在完全生成前开始发送）、缓存控制等。

# q
HTTP/2 在协议层面做了哪些改进来提升性能？
# a
采用二进制协议（不再使用纯文本），支持多路复用（同一连接上并行处理多个请求/响应），头部压缩等，目标为减少延迟、提高吞吐量并改善网页浏览体验。

