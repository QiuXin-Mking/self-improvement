# q
OSD是什么？
# a
Object Storage Device，对象存储设备。

# q
udev的作用是什么？
# a
设备管理器，位于驱动层之下，负责设备事件处理。

# q
文件描述符(fd)如何理解？
# a
每个文件一个类似标号，不是 id，是属性，用来表示一个打开的文件。

# q
relay在存储系统中指什么？
# a
重播，即重新加载日志，利用 mdlog 恢复脏数据。

# q
async和sync的含义及相互关系？
# a
async 为异步，sync 为同步，两者含义相反，异步常用于多线程场景。

