# q
如何使用 nc/ncat 命令检查 Windows 文件共享（445 端口）是否开放？
# a
使用命令：
```
nc -zv 172.31.0.23 445
```
`-z` 表示扫描模式（zero-I/O），不发送数据，仅检查端口是否开放；`-v` 表示详细输出（verbose），打印连接过程和结果。

# q
nc 命令的 `-z` 和 `-v` 参数分别有什么作用？
# a
`-z`：扫描模式（zero-I/O），不发送数据，只用来检查端口是否开放；
`-v`：详细输出（verbose），会打印出详细连接过程和结果。

