# q
介绍一个Linux进程的固定线性内存空间的构成。
# a
64M内存空间  
对应代码段 -> .text 主要是程序的代码以及编译时静态链接进来的库。  
对应常量字符段 -> .rodata 里存的是 常量字符串 等只读内容，  
对应全局变量字符段（static） -> .data 里存的是初始化的全局变量  
对应全局变量字符段 -> .bss 存的是未初始化的全局变量，在程序执行之前BSS 段会自动清 0。主要是为了节省程序的目标文件或者说可执行文件所占的磁盘空间。  
对应堆、栈段 -> 堆、栈

