# q
实现lock_guard需要具备哪四个关键特性？
# a
- 持有一个 mutex（或 mutex）的引用；
- 构造时加锁；
- 析构时自动释放锁；
- 禁止拷贝和赋值（防止一个锁对象被多次析构解锁）。

# q
lock_guard为什么要禁止拷贝和赋值？
# a
防止一个锁对象被多次析构解锁，导致未定义行为。

# q
lock_guard的析构函数应该执行什么操作？
# a
自动释放所持有 mutex 的锁。

