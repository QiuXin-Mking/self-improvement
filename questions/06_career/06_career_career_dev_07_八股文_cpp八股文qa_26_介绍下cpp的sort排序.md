# q
C++标准库的 sort 函数内部主要混合使用了哪三种排序算法？
# a
“小用插入，多用快排，太深用堆排”：
- 快速排序（Quick Sort）为主
- 小区间使用插入排序（Insertion Sort）优化
- 递归深度过大时转为堆排序（Heap Sort）避免最坏情况

# q
sort 在什么条件下会切换为插入排序？
# a
当待排序元素个数小于等于一定阈值（如 16 个）时，采用插入排序。

# q
sort 在什么条件下会触发堆排序？
# a
当递归深度过深，大约达到 `log₂N × 2` 倍左右时，为避免快排退化到 O(n²)，会切换为堆排序。

