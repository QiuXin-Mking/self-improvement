# q
内存的分配方式有几种？
# a
三种：静态分配、栈内存分配（自动分配）、堆内存分配（动态分配）。

# q
什么是静态分配？它有什么特点？
# a
静态分配在编译时由编译器分配内存，大小和生命周期在编译期间确定。例如全局变量、静态变量以及函数中的静态数组。

# q
栈内存分配的特点是什么？
# a
栈内存分配在程序运行时由系统自动为局部变量分配和回收内存，速度快，内存空间由系统自动管理。

# q
堆内存分配的特点是什么？
# a
堆内存分配在程序运行时由程序员通过如 `malloc`/`new` 等函数手动分配和释放内存，灵活但需自行管理。

