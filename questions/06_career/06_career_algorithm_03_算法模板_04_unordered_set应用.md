# q
`std::unordered_set` 的 `erase` 函数作用是什么？
# a
`erase` 用于删除 `std::unordered_set` 中的指定元素，例如 `mySet.erase(3)` 会删除值为 3 的元素。

# q
`std::unordered_set` 的 `insert` 函数如何使用？
# a
`insert` 用于向 `std::unordered_set` 中插入元素，例如 `mySet.insert(6)` 会将值为 6 的元素插入集合中。由于 `unordered_set` 存储唯一元素，插入已存在的值不会生效。

