# q
Ceph快速更新可能带来什么兼容性问题？
# a
可能导致内核和GCC版本不兼容。

# q
常见的Ceph部署工具有哪些？
# a
cephadm（容器化方式）、rook（基于Kubernetes）、ceph-ansible 和 ceph-deploy。

# q
在企业部署中，哪个Ceph版本被广泛使用？
# a
N版本（Nautilus）。

# q
ceph-deploy部署工具不支持哪个操作系统版本？
# a
不支持 CentOS 8。

