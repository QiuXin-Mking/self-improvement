# q
在Lustre LNet中，结构体 `struct lnet_peer` 的作用是什么？
# a
代表一个网络对端节点。

# q
在Lustre LNet中，结构体 `struct lnet_msg` 的作用是什么？
# a
代表一条网络消息。

# q
在Lustre LNet中，结构体 `struct lnet_route` 的作用是什么？
# a
路由表项。

# q
在Lustre LNet中，结构体 `struct lnet_libmd` 的作用是什么？
# a
内存描述符，与 RDMA 相关。

