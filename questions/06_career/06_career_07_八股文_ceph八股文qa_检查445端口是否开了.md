# q
如何使用 nc 命令检查远程主机（如 172.31.0.23）的 445 端口是否开放？
# a
```bash
nc -zv 172.31.0.23 445
```

# q
nc 命令中的 `-z` 和 `-v` 参数分别是什么作用？
# a
- `-z`：扫描模式（zero-I/O），不发送任何数据，仅用于检测端口是否开放。
- `-v`：详细输出（verbose），会打印出连接过程和结果，便于查看状态。

