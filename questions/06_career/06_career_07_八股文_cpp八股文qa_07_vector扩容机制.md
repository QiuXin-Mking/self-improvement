# q
vector底层使用什么样的内存布局？
# a
vector底层通过连续内存块存储元素，支持随机访问，属于动态数组。

# q
vector在什么情况下会触发扩容？
# a
当插入元素时，若当前容量（capacity()）不足以容纳新元素，便会触发扩容。常见触发函数包括 push_back()。

# q
reserve(n) 和 resize(n) 的区别是什么？
# a
reserve(n) 用于预留至少 n 个元素的容量，避免频繁扩容，但不改变元素数量；resize(n) 用于调整元素数量，若 n 大于当前容量，则可能触发扩容。

