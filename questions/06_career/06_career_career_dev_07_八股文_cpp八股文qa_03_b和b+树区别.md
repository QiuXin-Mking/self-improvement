# q
B树和B+树在数据存储位置上的本质区别是什么？
# a
B+树仅在叶子节点存储数据，非叶子节点仅存储索引；而B树在所有节点（叶子和非叶子）都存储数据。

# q
B+树的叶子节点之间是如何连接的？B树呢？
# a
B+树的所有叶子节点通过双向链表（或单向链表）连成一个有序序列；B树的叶子节点之间无关联，是孤立的。

# q
为什么在大并发查询场景下，B+树的性能高于B树？
# a
B+树的树深度更低、宽度更大、查询路径更短，导致每次查询的IO次数更少，因此在大并发查询时整体性能优于B树。

