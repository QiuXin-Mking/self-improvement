# q
解决vector在多线程并发push_back导致线程不安全的问题，有哪些典型方案？
# a
1. 使用mutex独占锁（读写锁）：适用于写少读多且需要强一致性（CAP中的Consistency）的场景。
2. 使用共享锁：适用于需要满足高可用（Availability）的场景。

# q
在使用锁解决STL容器线程不安全问题时，mutex独占锁和共享锁分别适用于什么场景？
# a
- mutex独占锁：适合写少读多、对强一致性要求高的场景。
- 共享锁：适合需要高可用的场景。

