# q
lists = [1, 1, 1, 3, 5]; lists.remove(1) 执行后，列表的内容是什么？
# a
[1, 1, 3, 5]。`remove` 移除列表中某个值的第一个匹配项，而不是所有匹配项。

# q
lists = [1, 1, 1, 3, 5]; lists.index(1) 的返回值是多少？
# a
0。`index` 返回列表中指定值第一个匹配项的索引位置。

