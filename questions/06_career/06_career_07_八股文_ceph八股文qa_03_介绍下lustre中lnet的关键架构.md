# q
在 Lustre LNet 中，代表网络对端节点的关键数据结构是什么？
# a
struct lnet_peer，代表一个网络对端节点。

# q
在 Lustre LNet 中，代表网络消息的关键数据结构是什么？
# a
struct lnet_msg，代表一条网络消息。

# q
在 Lustre LNet 中，路由表项对应的数据结构是什么？
# a
struct lnet_route，用于存储路由表项。

# q
在 Lustre LNet 中，与 RDMA 相关的内存描述符结构是什么？
# a
struct lnet_libmd，内存描述符，与 RDMA 相关。

