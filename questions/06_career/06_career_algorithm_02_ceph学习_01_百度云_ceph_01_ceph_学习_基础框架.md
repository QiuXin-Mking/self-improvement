# q
Ceph 主要提供哪几种存储接口？
# a
RBD（块存储）、RGW（对象存储）和 CephFS（文件系统）。

# q
Ceph N 版本有什么部署特点？
# a
每个组件以独立进程方式运行；通常在 Ubuntu 上使用 ceph-deploy 工具部署；需要匹配底层库和 GCC 版本；CentOS 7 上对应 N 版本。

# q
Ceph 存储池支持哪些高级管理功能？
# a
创建/删除实例、配额管理、快照管理、压缩管理和纠删码管理。

