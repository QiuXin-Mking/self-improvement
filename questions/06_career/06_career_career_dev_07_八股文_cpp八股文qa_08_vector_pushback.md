# q
vector 的 push_back 在什么条件下会触发扩容？
# a
当 `size() == capacity()` 时，即当前元素数量达到已分配的内存容量。

# q
push_back 触发扩容时的时间开销有多大？
# a
每次扩容需要拷贝所有已有元素，单次扩容时间复杂度为 O(n)。若频繁触发扩容，整体时间复杂度可能退化到 O(n²)。

# q
push_back 扩容会造成怎样的内存浪费？
# a
扩容后分配的新容量通常大于实际需要的元素数量，未使用的容量会占用额外内存，造成内存碎片和浪费。

